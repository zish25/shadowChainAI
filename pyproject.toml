[project]
name = "shadowchain-ai"
version = "0.1.0"
description = "Cybersecurity simulation environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "scikit-learn",
    "numpy",
    "openenv>=0.2.0"
]

[project.scripts]
server = "api:app"